name: Deploy
on: 
  push:
    branches:
    - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    name: deploy
    steps:

    - uses: actions/checkout@v4

    - name: Start minikube
      uses: medyagh/setup-minikube@latest

    - name: try it out
      run: ./scripts/apply.sh